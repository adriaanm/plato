[package]
authors = ["Bastien Dejean <nihilhill@gmail.com>"]
name = "plato"
version = "0.9.45"
edition = "2021"

[[bin]]
name = "plato"
path = "src/main.rs"

[dependencies]
if-addrs = "0.15.0"
mdns-sd = { version = "0.21.0", default-features = false }
plato-core = { path = "../core" }
# The pairing protocol, compiled into both ends so the wire cannot mismatch.
pairing = { path = "../pairing" }
